# ==============================================================================
# Docker Compose — Port Mappings (host:container)
# Credentials are managed in backend/.env (single source of truth)
# ==============================================================================

APP_PORT=8000
FRONTEND_PORT=3001
DB_PORT=3306
REDIS_PORT=6379
MAILPIT_PORT=8025
