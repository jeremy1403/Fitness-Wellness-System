NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Server-only: internal URL for container-to-container calls in Docker.
# Not needed for local dev (falls back to NEXT_PUBLIC_API_BASE_URL).
# Set via docker-compose environment; do NOT prefix with NEXT_PUBLIC_.
# BACKEND_URL=http://app:80
